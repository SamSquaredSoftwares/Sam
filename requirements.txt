anthropic>=1,<2
# agents/ and tls_trust call out over httpx. Declared explicitly because
# anthropic 1.0+ depends on httpx2, not httpx -- without this line httpx only
# arrives by accident, via snowflake-connector-python.
httpx>=0.27,<1
sema4ai-actions>=1.6.6
sema4ai-action-server>=3.2.0
snowflake-connector-python>=4.7.2
pytest>=8.0.0
# Test-only, and declared rather than inherited: tests/test_dependency_manifests.py
# imports both to compare this file against actions/package.yaml, and both
# otherwise arrive only transitively -- the same accident that hid httpx above.
pyyaml>=6
packaging>=23
